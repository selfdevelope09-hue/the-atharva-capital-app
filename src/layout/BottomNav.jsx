/**
 * Legacy layout nav — app shell uses `src/simple/BottomNavLite.jsx`.
 * Re-export keeps any old imports working without duplicating route lists.
 */
export { default, bottomNavHiddenPathLite as bottomNavHiddenPath } from '../simple/BottomNavLite';
