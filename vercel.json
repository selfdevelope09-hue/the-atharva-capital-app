{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "echo 'gh-pages: static files only, skip install'",
  "buildCommand": "echo 'gh-pages: static files only, skip build'",
  "outputDirectory": ".",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
